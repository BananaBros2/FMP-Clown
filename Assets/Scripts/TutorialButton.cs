using UnityEngine;
using UnityEngine.InputSystem;

public class TutorialButton : MonoBehaviour
{
    [SerializeField] private string actionName;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    //void Start()
    //{

    //    //textComp.text = originalText + "\n" + InputSystem.actions.FindAction(actionName).GetBindingDisplayString();

    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    //print(InputSystem.actions.FindAction(actionName));//.GetBindingDisplayString(InputBinding.MaskByGroup("Normal")));
    //    print(InputSystem.actions.FindAction(actionName).GetBindingDisplayString());

    //    //print(InputSystem.actions.FindAction(actionName).);
    //}
}
